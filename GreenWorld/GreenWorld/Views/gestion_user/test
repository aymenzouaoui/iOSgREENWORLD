private func verifyCode() {
    let fullCode = code1 + code2 + code3 + code4
    userViewModel.verifyResetCode(email: email, resetCode: fullCode) { result in
        DispatchQueue.main.async {
            switch result {
            case .success(_):  // You can use an underscore if you don't need the user object
                self.navigateToResetPassword = true
            case .failure(let error):
                self.showToast = true
                // Optionally, handle the error more specifically, e.g., update a message variable
                DispatchQueue.main.asyncAfter(deadline: .now() + 3) {
                    self.showToast = false
                }
            }
        }
    }
}
